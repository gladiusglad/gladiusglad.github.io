{
  "name": "frozendev-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "rmdir /s /q node_modules/.cache & next build && next export & cd out && copy nul .nojekyll > nul & cd ../ && gh-pages -d out -t true"
  },
  "dependencies": {
    "next": "10.0.6",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "sass": "^1.32.6",
    "webgl-fluid": "0.0.4"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
